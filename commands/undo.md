# Undo Commands in Git

## git reset --soft HEAD~1
Removes last commit but keeps changes staged.

## git reset --hard HEAD~1
Deletes last commit and removes changes.

## git checkout <commit-id>
Go back to a specific commit.
